# Project 3 — User Management API with SQLite

A RESTful API built with **Node.js**, **Express**, and **SQLite** (via `better-sqlite3`) that performs full CRUD operations with persistent storage and input validation.

---

## Tech Stack

- Node.js
- Express.js
- SQLite (`better-sqlite3`)

---

## Project Structure